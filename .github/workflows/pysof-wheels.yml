name: Build and Publish pysof Wheels

on:
  workflow_run:
    workflows: ['pysof CI']
    branches: [main, epic/pysof]
    types:
      - completed
  workflow_dispatch:
    inputs:
      publish:
        description: 'Publish to PyPI (only works on tags)'
        required: false
        default: false
        type: boolean

env:
  CARGO_TERM_COLOR: always
  MATURIN_PEP517_ARGS: --profile release

defaults:
  run:
    working-directory: crates/pysof

jobs:
  build-wheels:
    name: Build wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.runner }}
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    strategy:
      matrix:
        include:
          # Linux builds
          - os: Linux
            runner: [self-hosted, Linux]
            target: x86_64-unknown-linux-gnu
            platform: linux_x86_64
          - os: Linux
            runner: [self-hosted, Linux]
            target: x86_64-unknown-linux-musl
            platform: linux_x86_64
          # Windows builds
          - os: Windows
            runner: [self-hosted, Windows]
            target: x86_64-pc-windows-msvc
            platform: win_amd64
          # macOS builds (when available)
          - os: macOS
            runner: [self-hosted, macOS]
            target: x86_64-apple-darwin
            platform: macosx_10_9_x86_64
          - os: macOS
            runner: [self-hosted, macOS]
            target: aarch64-apple-darwin
            platform: macosx_11_0_arm64

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: 'latest'

      - name: Set up Python 3.11
        run: uv python install 3.11

      - name: Create virtual environment
        run: uv venv --python 3.11

      - name: Install maturin
        run: uv add maturin

      - name: Build wheel (Linux/macOS)
        if: runner.os != 'Windows'
        run: |
          uv run maturin build --release --target ${{ matrix.target }} --out dist

      - name: Build wheel (Windows)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          uv run maturin build --release --target ${{ matrix.target }} --out dist

      - name: Test wheel installation
        run: |
          # Install the built wheel
          uv pip install dist/*.whl --force-reinstall

          # Run basic import test
          uv run python -c "import pysof; print(f'pysof version: {pysof.__version__}')"

          # Run a subset of tests to verify functionality
          uv run python -m pytest tests/test_import.py tests/test_package_metadata.py -v

      - name: Upload wheel artifacts
        uses: actions/upload-artifact@v4
        with:
          name: wheel-${{ matrix.platform }}
          path: crates/pysof/dist/*.whl
          retention-days: 30

  # Detect if the triggering commit is tagged with a pysof release tag
  detect-tag:
    name: Detect tag for release
    runs-on: [self-hosted, Linux]
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    outputs:
      tag_name: ${{ steps.find-tag.outputs.tag_name }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}
          fetch-depth: 0
      - name: Fetch tags
        run: git fetch --tags --force
      - id: find-tag
        name: Find matching tag
        shell: bash
        run: |
          TAG=$(git tag --points-at HEAD | grep '^pysof-v' || true)
          if [ -n "$TAG" ]; then
            echo "Found tag: $TAG"
            echo "tag_name=$TAG" >> $GITHUB_OUTPUT
          else
            echo "No matching tag found"
            echo "tag_name=" >> $GITHUB_OUTPUT
          fi

  # Job to collect all wheels and create a release
  create-release:
    name: Create Release
    runs-on: [self-hosted, Linux]
    needs: [build-wheels, detect-tag]
    if: ${{ github.event.workflow_run.conclusion == 'success' && needs.detect-tag.outputs.tag_name != '' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Download all wheel artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Create release archive
        run: |
          mkdir -p release
          find dist -name "*.whl" -exec cp {} release/ \;
          cd release
          tar -czf ../pysof-wheels.tar.gz *.whl
          cd ..
          ls -la release/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ needs.detect-tag.outputs.tag_name }}
          name: pysof ${{ needs.detect-tag.outputs.tag_name }}
          body: |
            Python wheels for pysof ${{ github.ref_name }}

            ## Installation

            Download the appropriate wheel for your platform and install with:
            ```bash
            pip install pysof-*.whl
            ```

            ## Supported Platforms
            - Linux x86_64 (glibc and musl)
            - Windows x86_64
            - macOS x86_64 and ARM64

            ## Python Version
            - Python 3.11 only
          files: |
            release/*.whl
            pysof-wheels.tar.gz
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Job to publish to PyPI (only on tags and when explicitly requested)
  publish-pypi:
    name: Publish to PyPI
    runs-on: [self-hosted, Linux]
    needs: [build-wheels, detect-tag]
    if: |
      github.event.workflow_run.conclusion == 'success' &&
      needs.detect-tag.outputs.tag_name != '' &&
      (github.event_name == 'workflow_dispatch' && github.event.inputs.publish == 'true')

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Download all wheel artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: 'latest'

      - name: Set up Python 3.11
        run: uv python install 3.11

      - name: Create virtual environment
        run: uv venv --python 3.11

      - name: Install build tools
        run: |
          uv add maturin
          uv add twine

      - name: Build source distribution
        run: uv run maturin sdist --out dist

      - name: Publish to PyPI
        run: |
          # Upload wheels and source distribution
          uv run twine upload dist/*.whl dist/*.tar.gz
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}

  # Job to test wheel builds on different Python versions (when available)
  test-wheels:
    name: Test wheels on ${{ matrix.python-version }}
    runs-on: [self-hosted, Linux]
    needs: build-wheels
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'pull_request' }}

    strategy:
      matrix:
        python-version: ['3.11']
        # Add more Python versions as they become supported

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download Linux wheel artifacts
        uses: actions/download-artifact@v4
        with:
          name: wheel-linux_x86_64
          path: dist

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: 'latest'

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Create virtual environment
        run: uv venv --python ${{ matrix.python-version }}

      - name: Install wheel
        run: uv pip install dist/*.whl --force-reinstall

      - name: Run comprehensive tests
        run: |
          # Install test dependencies
          uv add pytest pytest-cov

          # Run all tests
          uv run python -m pytest tests/ -v --tb=short
