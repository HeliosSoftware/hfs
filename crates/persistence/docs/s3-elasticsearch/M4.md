# M4 - Docs Finalization and Release Readiness

## Goal
Finalize user/operator docs and ensure release-facing guidance is complete for `s3-elasticsearch`.

## Implemented Tasks

1. Persistence README updates.
- File:
  - `crates/persistence/README.md`
- Purpose:
  - Mark `s3-elasticsearch` as implemented in search offloading documentation.
  - Document startup configuration, failure semantics, reindex runbook, and local MinIO+ES recipe.
  - Link milestone and implementation docs for maintainers.

2. Root README backend docs updates.
- File:
  - `README.md`
- Purpose:
  - Add `S3 + Elasticsearch` to supported backend matrix.
  - Add startup example and broaden environment variable table to include S3+ES and reindex knobs.

3. Consolidated implementation write-up.
- File:
  - `crates/persistence/docs/s3-elasticsearch/IMPLEMENTATION.md`
- Purpose:
  - Capture architecture decisions, tradeoffs, CI matrix, operational knobs, risks, and acceptance checklist.

## Validation
- `cargo fmt --all`
- `cargo test -p helios-rest`
- `cargo test -p helios-hfs --features s3,elasticsearch`
- `cargo test -p helios-persistence --features s3,elasticsearch --test composite_s3_elasticsearch_tests`
- `cargo test -p helios-persistence --features s3,elasticsearch --test elasticsearch_tests -- --skip es_integration`
- `cargo test -p helios-persistence --features s3,elasticsearch --test minio_s3_elasticsearch_tests`
- `cargo clippy -p helios-persistence -p helios-rest -p helios-hfs --all-targets --features s3,elasticsearch -- -D warnings -A clippy::items_after_test_module -A clippy::large_enum_variant -A clippy::question_mark -A clippy::collapsible_match -A clippy::collapsible_if -A clippy::field_reassign_with_default -A clippy::doc-overindented-list-items -A clippy::doc-lazy-continuation`

