[package]
name = "helios-serde"
version = "0.1.0"
edition = "2021"
description = "Serialization and deserialization support for FHIR resources across multiple formats"
license = "MIT OR Apache-2.0"
repository = "https://github.com/HeliosSoftware/hfs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
# Add any dev dependencies needed for tests
